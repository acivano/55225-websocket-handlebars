
<main>
    <section  class="container seccionDonacion">
        <div id="contenedorPadre" class="d-flex flex-column align-content-center justify-content-start gap-4 flex-lg-row flex-lg-wrap mt-5">
        {{!-- {{#each products}}
            <div class="d-flex justify-content-center">
                <div class="seccion_datos">
                        <div class="card-body d-flex flex-column align-content-center justify-content-center">
                            <img class="img-fluid" src="{{this.thumbnail}}" alt="{{this.title}}">
                        </div>
                        <div>
                            <p class="card-text text-center mt-1 altotexto">{{this.title}}</p>
                            <h3 class="montoDonacion text-center mt-1">${{this.price}}</h3>
                            <h6 id="idProducto" class="display-none">{{this.code}}</h6>
                            <button  class="botonPersonalizado mt-1" onclick="addProductoCarrito({{this._id}})">Agregar</button>
                        </div>
                </div>
            </div>
        {{/each}}     --}}
        </div>
    </section>
</main> 
<script src="/socket.io/socket.io.js"></script>
